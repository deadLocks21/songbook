{
  "name": "songbook-release",
  "private": true,
  "description": "Outillage semantic-release uniquement (versioning CI). N'a rien à voir avec l'app Flutter.",
  "devDependencies": {
    "@semantic-release/exec": "^7.0.3",
    "@semantic-release/git": "^10.0.1",
    "conventional-changelog-conventionalcommits": "^8.0.0",
    "semantic-release": "^24.2.0"
  }
}
